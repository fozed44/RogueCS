namespace RogueCS.Engine.Implementations.StateHandlers {

	/// <summary>
	/// Enumerates the possible game states. Each state has its own state handler
	/// implementation.
	/// </summary>
	public enum GameState {
		IntroScreen,
		Dungeon,
		Close
	};
}
